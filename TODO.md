# TODO

Open work for this site. Cursor rules stay constraints only — put backlog here.

## Dining Timeline — under-card comments

Optional personal caption under Timeline cards only.

- Optional `comment` string on the visit in `michelin.json` / `award.json`. Dual-listed: same `id`, one comment (copy on merge like `accolades`).
- Hang 1–2 lines under the card on the page background — not inside the white card (rail stays through card centers). Same 260px width, muted type, ellipsis if longer. No empty box when omitted.
- Fade with the card. Michelin / Guide / Award grids unchanged. Do not backfill all visits.
